{
  "name": "@molemap/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build && wrangler dev",
    "build": "vite build",
    "test": "vitest run",
    "deploy": "vite build && wrangler deploy",
    "migrate": "wrangler d1 migrations apply molemap --remote"
  },
  "dependencies": {
    "@sparkjsdev/spark": "^0.1.10",
    "hono": "^4.6.0",
    "three": "^0.178.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.0",
    "@types/three": "^0.178.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
