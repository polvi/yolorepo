[package]
name = "tlc-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "TLA+ parser, level checker, and finite-state safety model checker (safety subset of SANY + TLC)"

[dependencies]
hashbrown = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
