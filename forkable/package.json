{
  "name": "forkable",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test packages && bun run --cwd apps/web test",
    "deploy": "bun run --filter './apps/*' deploy"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
