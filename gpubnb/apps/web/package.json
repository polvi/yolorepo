{
  "name": "@gpubnb/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build && wrangler dev",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "vite build && wrangler deploy",
    "migrate": "wrangler d1 migrations apply gpubnb --remote"
  },
  "dependencies": {
    "@gpubnb/client": "workspace:*",
    "@gpubnb/protocol": "workspace:*",
    "@simplewebauthn/browser": "^13.2.0",
    "hono": "^4.6.0",
    "uqr": "^0.1.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
