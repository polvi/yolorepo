\* Passing config (complete search, 61625 states / 15721 distinct, depth 6).
\* MaxExpenses shrunk 2 -> 1 after ProposeCash blew up the state space past
\* the checker budget; the complementary shrink (MaxExpenses = 2 with a
\* single payment id p1) also passes complete (37357 / 15025, depth 5),
\* covering the multi-expense stale-suggestion race.
CONSTANTS
  Users = {u1, u2, u3}
  MaxAmt = 2
  MaxExpenses = 1
  PaymentIds = {p1, p2}
  NULL = NULL

SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT Conservation
INVARIANT SharesExact
INVARIANT SettlementSound
INVARIANT IdempotentPayments
PROPERTY NoOverwrite
CHECK_DEADLOCK FALSE
